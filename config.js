/**
 * Arquivo de configuração para comandos do bot
 * Configuration file for bot commands
 */

/**
 * Prefixo usado para identificar comandos
 * Prefix used to identify commands
 * Exemplo: Se PREFIX = "!", então comandos começam com "!"
 * Example: If PREFIX = "!", then commands start with "!"
 */
export const PREFIX = "!";

/**
 * Nome do bot
 * Bot name
 */
export const BOT_NAME = "Baileys Bot";

/**
 * Número do dono do bot (formato: número@s.whatsapp.net)
 * Bot owner number (format: number@s.whatsapp.net)
 * Exemplo: "5511999999999@s.whatsapp.net"
 */
export const OWNER_NUMBER = "";
